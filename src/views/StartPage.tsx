import { defineComponent } from "vue";


export const StartPage = defineComponent({
  setup: (props, context) => {
    return () => (
        <div>
            startpage
        </div>
    );
  },
});